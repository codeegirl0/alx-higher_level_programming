#!/usr/bin/python3
# 5-no_c.py


def no_c(my_string):
    """Deleting c and C in a string."""
    for i in range(len(my_string):
        if my_string[i] != 'c' or i != 'C':
            print(my_string[i])
